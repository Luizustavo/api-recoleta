services:
  - type: web
    name: api-recoleta
    env: node
    plan: free
    buildCommand: npm ci --omit=dev
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --max-old-space-size=460
